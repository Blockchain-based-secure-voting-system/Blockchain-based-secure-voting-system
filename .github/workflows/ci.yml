name: CI - Security & Build Checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Run Unit Tests & Cryptographic Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: clippy

      - name: Cache Cargo Dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run Unit Tests (Cryptographic Suite & CLI)
        run: cargo test --package voting_crypto --package cli --verbose

      # Canister only — mio/tokio-based crates (cli) must never be built against wasm32-unknown-unknown
      - name: Check Canister Wasm Target Build
        run: cargo check --package canister --target wasm32-unknown-unknown --verbose

      - name: Run Cargo Clippy (Native CLI & Crypto Crate)
        run: cargo clippy --package voting_crypto --package cli -- -D warnings

      # Canister only — mio/tokio-based crates (cli) must never be built against wasm32-unknown-unknown
      - name: Run Cargo Clippy (Wasm Canister)
        run: cargo clippy --package canister --target wasm32-unknown-unknown -- -D warnings
