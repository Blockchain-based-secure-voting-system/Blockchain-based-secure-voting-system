name: CI - Security & Build Checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Run Unit Tests & Cryptographic Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: clippy

      - name: Cache Cargo Dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run Unit Tests (Cryptographic Suite & CLI)
        run: cargo test --package voting_crypto --package cli --verbose

      - name: Check Canister Wasm Target Build
        run: cargo check --package canister --target wasm32-unknown-unknown --verbose

      - name: Run Cargo Clippy (Wasm Canister & Native Crates)
        run: cargo clippy --workspace --target wasm32-unknown-unknown -- -D warnings
